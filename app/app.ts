'use strict';

// var variable: string                  = "process.env.DBNAME";
// var dbname                  = process.env.DBNAME;
// var port                    = process.env.PORT || 4000;

// set variables for environment
// import {traceur} from               'traceur';
// import {express} from               'express';
// import {path} from                  'path';
// import {initMongo} from             __dirname + '/lib/init-mongo.js';
// import {initRoutes} from            __dirname + '/lib/init-routes.js';
// var app                             = express();

import {bootstrap} from             '@angular/platform-browser-dynamic';
// import {AppComponent} from          './components/app.component';
// bootstrap(AppComponent);

// instruct express to server up static assets
// app.use(express.static(__dirname + '/public'));

// // views as directory for all template files
// app.set('views', path.join(__dirname, 'views'));
// app.set('view engine', 'jade');

// app.use('/js/vendor', express.static(__dirname + '/js/vendor'));
// app.use('/css/vendor', express.static(__dirname + '/css/vendor'));
// app.use(initMongo.connect);
// app.use(initRoutes);

// var server = require('http').createServer(app);
// server.listen(port, function(){
//   console.log('Node server listening. Port: ' + port + ', Database: ' + dbname);
// });

// module.exports = app;
